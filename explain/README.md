# explain

Turn model outputs and drift reports into short rows beginners can scan.

Examples:

- `examples/demo.ns` gives a compact multi-function tour.
- `examples/prediction_factors.ns` focuses on `summarizePrediction()`.
- `examples/drift_summary.ns` shows `summarizeDrift()`.
- `examples/backtest_summary.ns` shows `summarizeBacktest()`.
